import {images} from './images';
import * as settings from './settings';

const events = {
  KEYDOWN: 'keydown',
  KEYUP: 'keyup',
}

const keys = {
  BACKSPACE: 'Backspace',
  ENTER: 'Enter',
}

export {
  images,
  settings,
  events,
  keys
};
